services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: loop
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - loop_postgres_data:/var/lib/postgresql/data

volumes:
  loop_postgres_data:

